# SSL Certificates

This folder is for SSL certificates in production.

For local development, nginx runs on HTTP (port 80).
For production, place your SSL certificates here:
- `cert.pem` - SSL certificate
- `key.pem` - SSL private key
